"""Направления: чей вопрос разбирают, по закрытому списку из файла."""

from minuteforge.blocks import Block
from minuteforge.directory import (
    DEFAULT_LABEL,
    EMPTY,
    Directory,
    Entry,
    _phrases,
    at,
    is_announcement,
    read_directory,
    who_leads,
)


def directory(label: str = DEFAULT_LABEL) -> Directory:
    """Справочник для проверок: никакой предметной области, одни выдумки."""
    return Directory(
        entries=(
            Entry("Северный филиал", ("северн",)),
            Entry("Заречный филиал", ("заречн",)),
            Entry("Восточная площадка", ("восточн",)),
            Entry("Садовый участок", ("сад",)),
        ),
        label=label,
    )


def test_a_name_is_recognised_through_its_cases():
    """«Северного», «Северному», «Северном» — одно направление, а
    морфологии для этого не нужно."""
    units = directory()
    assert units.find("Следующий филиал Северный") == "Северный филиал"
    assert units.find("Коллеги Заречного филиала, просьба подтвердить") == "Заречный филиал"
    assert units.find("Далее Восточная площадка на примере цеха") == "Восточная площадка"


def test_a_mangled_name_is_not_a_unit():
    """Распознавание коверкает названия, и это главный довод за список.

    Неверный адресат хуже пустого: пустой заставляет уточнить перед
    рассылкой, неверный уходит в рассылку как есть.
    """
    assert directory().find("Следующий филиал Сивернай") == ""
    assert directory().find("Далее Заподная площадка") == ""


def test_a_name_is_matched_from_the_start_of_a_word():
    """Совпадение искали подстрокой где угодно внутри слова, и в графе
    появлялось направление, которого в разговоре не было."""
    assert directory().find("Это очень досадно") == ""
    assert directory().find("Пересеверный вариант нам не подходит") == ""


def test_a_short_name_needs_the_end_of_the_word_too():
    """У короткой основы хвост должен быть длиной с падежное окончание:
    граница слова есть и у «сада», и у «садитесь»."""
    units = directory()
    assert units.find("Сад на связи") == "Садовый участок"
    assert units.find("По саду замечаний нет") == "Садовый участок"
    assert units.find("Садитесь, пожалуйста") == ""


def test_the_unit_holds_until_the_next_one():
    """Объявляют один раз, а поручают потом четверть часа."""
    marks = directory().follow([
        Block("SPEAKER_02", "Следующий филиал Северный.", 100, 120),
        Block("SPEAKER_08", "Готовность 84%. Просьба подтвердить срок.", 200, 260),
        Block("SPEAKER_02", "Следующий филиал Заречный.", 400, 420),
    ])

    assert at(marks, 250) == "Северный филиал"
    assert at(marks, 450) == "Заречный филиал"
    assert at(marks, 50) == "", "до первого перехода адресата нет"


def test_the_name_is_given_in_the_next_phrase():
    """Объявляют и называют порознь: «Следующая площадка.» — и уже
    следующей фразой «Иван Иванович, Восточная». Пока смотрели только фразу
    перехода, начало совещания оставалось без направления."""
    marks = directory().follow([
        Block("SPEAKER_02", "Хорошо. Следующая площадка.", 100, 120),
        Block("SPEAKER_12", "Иван Иванович, Восточная. Коротко доложу.", 130, 200),
    ])

    assert at(marks, 150) == "Восточная площадка"


def test_an_unreadable_announcement_clears_the_unit():
    """Объявление, в котором названия не разобрать, — тоже переход.
    Оставить прежнее значило бы подписать чужие поручения тому, кого уже
    разобрали."""
    marks = directory().follow([
        Block("SPEAKER_02", "Следующий филиал Северный.", 100, 120),
        Block("SPEAKER_02", "Следующая площадка Заподная.", 300, 320),
        Block("SPEAKER_02", "Организуйте сейчас фотоотчёт.", 400, 420),
    ])

    assert at(marks, 200) == "Северный филиал"
    assert at(marks, 400) == ""


def test_a_quiet_move_counts_only_when_the_name_is_given():
    """«Далее Восточная площадка» — переход. «И так далее» — конец
    перечисления, и по нему разбор уходил туда, что упомянуто ниже
    мимоходом."""
    marks = directory().follow([
        Block("SPEAKER_12", "Далее Восточная площадка на примере цеха.", 100, 130),
        Block("SPEAKER_02", "Рассказывает стройнадзор и так далее.", 200, 230),
        Block("SPEAKER_02", "Как коллеги с Заречного заявляют, объект ведём.", 240, 280),
    ])

    assert at(marks, 250) == "Восточная площадка"


def test_a_move_is_proposed_as_often_as_it_is_announced():
    """«Предлагаю перейти к Заречному филиалу» — такой же переход, как
    «переходим к». На записи 17.09 это был единственный потерянный доклад:
    неопределённая форма в списке не значилась."""
    marks = directory().follow([
        Block("ведущий", "Предлагаю перейти к Заречному филиалу.", 100, 130),
        Block("докладчик", "План выполнен, отставаний нет.", 130, 400),
    ])

    assert at(marks, 200) == "Заречный филиал"


def test_the_next_something_else_is_not_an_announcement():
    """«Следующая группа — временные объекты по заречному закону»: название
    названо восьмым словом и не в своей роли — это разговор, а не переход.

    На записи 17.09 ровно по такой фразе чужому направлению досталось
    двадцать минут общей части: справочник находил название где угодно во
    фразе, а «следующее» в речи звучит поминутно.
    """
    units = directory()
    assert not is_announcement(
        "Следующая группа – это группа временных объектов по заречному закону.",
        units,
    )
    # А вплотную — объявление, даже когда единица не названа.
    assert is_announcement("Следующая у нас Восточная площадка.", units)
    assert is_announcement("Перейду к следующему филиалу, к Заречному.", units)


def test_a_name_said_before_the_word_next_is_not_an_announcement():
    """«Северный филиал начнёт работать в апреле следующего года» — это
    срок, а не переход: название стоит раньше слова, а не за ним."""
    assert not is_announcement(
        "Северный филиал начнет работать в новом формате в апреле следующего года.",
        directory(),
    )


def test_a_review_marks_every_unit_by_its_own_mention():
    """«Далее остановлюсь на филиалах, где отставание» — не переход к
    первому названному, а обзор нескольких: дальше их перебирают подряд, и
    своего объявления не будет ни у одного.

    На записи 17.09 такая фраза отдала первому названному восемнадцать
    минут, из которых его собственных было две: в остальных шла речь о
    четырёх других направлениях.
    """
    marks = directory().follow([
        Block("ведущий", "Далее остановлюсь на филиалах, где есть отставание.", 100, 160),
        Block("ведущий", "В Северном филиале закуплено шесть единиц из десяти.", 160, 220),
        Block("ведущий", "Отдельно обращу внимание на Заречный филиал.", 220, 300),
    ])

    assert at(marks, 100) == ""
    assert at(marks, 180) == "Северный филиал"
    assert at(marks, 260) == "Заречный филиал"


def test_inside_a_review_a_list_and_a_passing_mention_move_nothing():
    """Обзор перебирает по одному и полным названием. Список благополучных
    — «Северный, Заречный и Садовый участок укладываются в срок» — не
    переход к первому из них, а «в Северном опять сбои» посреди чужого куска
    — упоминание, а не начало разбора."""
    marks = directory().follow([
        Block("ведущий", "Далее остановлюсь на филиалах, где есть отставание.", 100, 160),
        Block("ведущий", "Северный, Заречный и Садовый участок укладываются в срок.", 160, 220),
        Block("ведущий", "В Восточной площадке отставание третий месяц.", 220, 280),
        Block("ведущий", "А в Северном опять сбои.", 280, 340),
    ])

    assert at(marks, 200) == ""
    assert at(marks, 260) == "Восточная площадка"
    assert at(marks, 320) == "Восточная площадка"


def test_a_review_ends_where_the_round_begins():
    """Обзор кончается обычным переходом: «Начнем с Заречного филиала» —
    это уже разбор по кругу, и упоминания больше отметку не двигают."""
    marks = directory().follow([
        Block("ведущий", "Сегодня остановимся на таких филиалах, как Северный и Заречный.", 100, 160),
        Block("ведущий", "Начнем с Заречного филиала.", 160, 220),
        Block("докладчик", "В Северном филиале у нас работы по графику.", 220, 280),
    ])

    assert at(marks, 180) == "Заречный филиал"
    assert at(marks, 260) == "Заречный филиал"


def test_a_move_counts_in_any_person_of_the_verb():
    """«Перехожу к» — тот же переход, что «переходим к»: лицо у глагола
    выбирают не задумываясь. На записи 10.09 на «Дальше перехожу к городу
    Омск» разметка потеряла двенадцатиминутный доклад."""
    marks = directory().follow([
        Block("ведущий", "Дальше перехожу к площадке Восточной.", 100, 160),
        Block("докладчик", "Работы идут по графику, отставаний нет.", 160, 300),
    ])

    assert at(marks, 200) == "Восточная площадка"


def test_a_bare_дальше_is_not_a_move():
    """«Дальше, Иван Иванович.» — передача слова, а не переход. Книжное
    «далее» так не употребляют, разговорное «дальше» — сплошь и рядом, и
    разметка брала название из соседней фразы, начиная разбор того, кого в
    повестке не было."""
    marks = directory().follow([
        Block("ведущий", "Дальше, Иван Иванович.", 100, 130),
        Block("докладчик", "По Садовому участку у нас вопрос закрыт.", 130, 300),
    ])

    assert at(marks, 200) == ""


def test_moving_to_a_slide_does_not_clear_the_unit():
    """«Переходим к фотоматериалу» сказано посреди разбора, и стирать по
    нему нельзя: направление не менялось."""
    marks = directory().follow([
        Block("SPEAKER_12", "Далее Восточная площадка на примере цеха.", 100, 130),
        Block("SPEAKER_12", "Переходим к фотоматериалу. Видим навалы.", 200, 260),
    ])

    assert at(marks, 250) == "Восточная площадка"


def test_counting_your_own_questions_is_not_a_move():
    """«Следующий вопрос уже прозвучал» — ведущий считает свои вопросы к
    тому же самому направлению, а не уходит с него."""
    marks = directory().follow([
        Block("SPEAKER_02", "Следующий филиал Северный.", 100, 130),
        Block("SPEAKER_02", "Следующий вопрос уже прозвучал. Финансирование есть?", 200, 260),
    ])

    assert at(marks, 250) == "Северный филиал"


def test_moving_to_another_item_of_the_agenda_clears_the_unit():
    """А переход к следующему вопросу повестки — конец разбора. Иначе
    поручения общей части достаются тому, кого разбирали до неё."""
    marks = directory().follow([
        Block("SPEAKER_02", "Следующий филиал Северный.", 100, 130),
        Block("SPEAKER_02", "Переходим к следующему вопросу, уважаемые коллеги.", 300, 330),
        Block("SPEAKER_11", "Прошу обратить внимание на нормативные акты.", 400, 430),
    ])

    assert at(marks, 200) == "Северный филиал"
    assert at(marks, 420) == "", "общая часть — не Северный филиал"


def test_an_announcement_inside_a_long_reply_counts_from_its_place():
    """Реплика ведущего бывает в две минуты, и объявление в её конце,
    отнесённое к началу, отдавало следующему всё, что поручено в этой же
    реплике предыдущему."""
    marks = directory().follow([
        Block("SPEAKER_02", "Следующий филиал Северный.", 0, 10),
        Block(
            "SPEAKER_02",
            "Дату ввода объекта назовите. " + "Разбираемся дальше. " * 40
            + "Следующий филиал Заречный.",
            100, 200,
        ),
    ])

    assert at(marks, 110) == "Северный филиал", "начало реплики — ещё прежнее"
    assert at(marks, 199) == "Заречный филиал"


def test_an_address_is_told_apart_from_a_mention():
    """«Коллеги Заречного филиала, просьба подтвердить» — обращение, а
    «Заречный филиал поддерживает предложение» — упоминание в докладе."""
    units = directory()
    assert units.starts_with("Коллеги Заречного филиала, просьба подтвердить") == "Заречный филиал"
    assert units.starts_with("Администрация Восточной площадки, дайте оценку") == (
        "Восточная площадка"
    )
    assert units.starts_with("Отмечу, что Заречный филиал выполнил план") == ""


def test_a_call_to_report_is_an_announcement():
    """Слово передают окликом: назвали — и человек начинает доклад.

    Оборота «переходим к» при этом не звучит вовсе, а разбор начинается.
    """
    units = directory()
    assert units.called_on("Северный филиал.") == "Северный филиал"
    assert units.called_on("Заречный филиал, пожалуйста.") == "Заречный филиал"
    assert units.called_on("Восточная площадка, прошу.") == "Восточная площадка"


def test_a_call_is_told_apart_from_talking_about_a_unit():
    """После названия не должно остаться ничего, кроме слова вызова.

    Иначе окликом станет и перекличка, и перечисление в чужом докладе, а
    разбор уйдёт к тому, кто своей очереди не дождался.
    """
    units = directory()
    assert units.called_on("Северный филиал на связи.") == ""
    assert units.called_on("Северный филиал и Заречный филиал.") == ""
    assert units.called_on("Заречному филиалу требуется новая смета.") == ""
    assert units.called_on("Восточная площадка слышите, да?") == ""


def test_a_call_starts_the_round():
    """Оклик двигает разбор так же, как объявление, и название в нём уже
    названо — вперёд по соседним фразам его искать не нужно."""
    units = directory()
    blocks = [
        Block("ведущий", "Переходим к первому вопросу повестки.", 0, 5),
        Block("ведущий", "Северный филиал, пожалуйста.", 5, 10),
        Block("SPEAKER_01", "План выполнен, отставаний нет.", 10, 60),
        Block("ведущий", "Заречный филиал.", 60, 70),
        Block("SPEAKER_02", "У нас закуплено двадцать единиц техники.", 70, 120),
    ]
    marks = units.follow(blocks)
    assert [name for _, name in marks][-2:] == ["Северный филиал", "Заречный филиал"]
    assert at(marks, 30) == "Северный филиал"
    assert at(marks, 100) == "Заречный филиал"


def test_a_call_from_the_floor_does_not_start_the_round():
    """На перекличке тем же окликом отзываются сами участники.

    «Северный филиал.» с тридцатой минуты — это ответ на проверку связи, а
    не передача слова, и по одной фразе они неразличимы. Различает голос:
    слово передаёт тот, кто ведёт.
    """
    units = directory()
    blocks = [
        Block("ведущий", "Коллеги, начнём проверку связи.", 0, 5),
        Block("SPEAKER_07", "Северный филиал.", 5, 10),
        Block("SPEAKER_08", "Заречный филиал на связи.", 10, 15),
        Block("ведущий", "Переходим к первому вопросу повестки.", 15, 20),
    ]
    assert [name for _, name in units.follow(blocks)] == [""]


def test_the_chair_leads_even_without_a_formula():
    """Председатель ведёт по должности, а не по тому, сказал ли он
    «переходим к». Известен он — его оклик засчитывается."""
    units = directory()
    blocks = [
        Block("Петров", "Северный филиал, пожалуйста.", 0, 10),
        Block("SPEAKER_01", "План выполнен, отставаний нет.", 10, 60),
    ]
    assert units.follow(blocks) == []
    assert [name for _, name in units.follow(blocks, hosts=["Петров"])] == [
        "Северный филиал"
    ]


def test_who_leads_finds_the_hosts_and_not_the_floor():
    """Ведущих несколько: общую часть ведёт председатель, разделы — свои
    докладчики. Искать одного было бы ошибкой."""
    units = directory()
    blocks = [
        Block("ведущий", "Переходим ко второму вопросу повестки.", 0, 10),
        Block("докладчик", "Следующий филиал Заречный.", 10, 20),
        Block("SPEAKER_07", "Северный филиал.", 20, 25),
    ]
    assert who_leads(_phrases(blocks), units) == {"ведущий", "докладчик"}


def test_without_a_directory_nothing_is_invented():
    """Нет файла — графа пустая. Услышанное название не берётся: его
    коверкает распознавание, а неверный адресат хуже пустого."""
    assert not EMPTY
    assert EMPTY.find("Далее Восточная площадка") == ""
    assert EMPTY.follow([Block("S", "Следующая площадка Восточная.", 0, 10)]) == []


def test_the_directory_is_read_from_a_file(tmp_path):
    """Файл правят в Excel: точка с запятой, заголовок, запятые внутри."""
    file = tmp_path / "справочник.csv"
    file.write_text(
        "Направление;Как звучит\n"
        "Северный филиал;северн, Дальний цех\n"
        "Заречный филиал;южн\n",
        encoding="utf-8-sig",
    )
    units = read_directory(file, label="Площадка")

    assert len(units) == 2
    assert units.label == "Площадка"
    assert units.find("Далее Северный филиал") == "Северный филиал"
    assert units.find("Проверим Дальний цех") == "Северный филиал", "синоним — то же самое"


def test_a_directory_without_a_second_column_still_works(tmp_path):
    """Как звучит — необязательно: без него узнаётся само название."""
    file = tmp_path / "справочник.csv"
    file.write_text("Северный филиал\nЗаречный филиал\n", encoding="utf-8")

    assert read_directory(file).find("Далее Северный филиал") == "Северный филиал"


def test_a_missing_directory_does_not_stop_the_work(tmp_path):
    """Протокол собирается и без графы: падать из-за справочника нельзя."""
    assert not read_directory(tmp_path / "нет-такого.csv")
    assert not read_directory(None)
    assert not read_directory("")


def test_the_third_column_says_how_to_address(tmp_path):
    """«Ставропольский край» → «Правительству Ставропольского края»: по-русски
    это другой падеж, а падежей инструмент не знает."""
    file = tmp_path / "справочник.csv"
    file.write_text(
        "Направление;Как звучит;Кому адресовать\n"
        "Первая площадка;первая площадк, северный цех;Руководству первой площадки\n"
        "Второй участок;второй участ\n",
        encoding="utf-8",
    )
    units = read_directory(file)

    assert units.addressees() == {"Первая площадка": "Руководству первой площадки"}
    assert units.find("Северный цех отчитался") == "Первая площадка", "созвучия целы"
