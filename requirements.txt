# Основное: чтение данных, обращение к локальной модели, сборка протокола.
# Этого достаточно, чтобы собрать протокол из готовой стенограммы —
# видеокарта не нужна.
#
#   pip install -r requirements.txt

loguru>=0.7
omegaconf>=2.3
requests>=2.31

# Списки участников приходят и в pdf: формат выбирает тот, кто рассылает, и
# просить его переделывать — значит получать список позже, чем нужно.
pypdf>=4.0

# Интерфейс. Без него остаётся командная строка.
streamlit>=1.28
